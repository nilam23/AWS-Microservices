AuctionsTableIAM:
  Effect: Allow
  Action:
    # - dynamodb:*
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
  Resource:
    - ${self:custom.AuctionsTable.arn}